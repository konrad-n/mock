<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SledzSpecke.App.ViewModels.Internships"
             x:DataType="vm:AddEditNewSMKInternshipViewModel"
             x:Class="SledzSpecke.App.Views.Internships.AddEditNewSMKInternshipPage"
             Title="{Binding Title}">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">
            <Label Text="{Binding InternshipName}" 
                   FontSize="18" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center" />

            <Label Text="Placówka realizująca szkolenie" FontAttributes="Bold" />
            <Entry Text="{Binding RealizedInternship.InstitutionName}" Placeholder="Np. Katedra i Klinika Psychiatryczna" />

            <Label Text="Nazwa komórki organizacyjnej (miejsce odbywania stażu)" FontAttributes="Bold" />
            <Entry Text="{Binding RealizedInternship.DepartmentName}" Placeholder="Np. Oddział psychiatryczny" />

            <Label Text="Liczba dni" FontAttributes="Bold" />
            <Entry Text="{Binding RealizedInternship.DaysCount}" Keyboard="Numeric" />

            <Label Text="Daty realizacji" FontAttributes="Bold" />
            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <VerticalStackLayout Grid.Column="0">
                    <Label Text="Od:" />
                    <DatePicker Date="{Binding RealizedInternship.StartDate}" Format="dd.MM.yyyy" />
                </VerticalStackLayout>
                <VerticalStackLayout Grid.Column="1">
                    <Label Text="Do:" />
                    <DatePicker Date="{Binding RealizedInternship.EndDate}" Format="dd.MM.yyyy" />
                </VerticalStackLayout>
            </Grid>

            <Button Text="Przelicz dni" Command="{Binding RecalculateDaysCommand}" 
                    BackgroundColor="Transparent" TextColor="{StaticResource Primary}" />

            <Label Text="Prowadzący staż (opcjonalnie)" FontAttributes="Bold" />
            <Entry Text="{Binding RealizedInternship.SupervisorName}" Placeholder="Np. dr Jan Kowalski" />

            <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,15,0,0">
                <Button Grid.Column="0" Text="Anuluj" Command="{Binding CancelCommand}" />
                <Button Grid.Column="1" Text="Zapisz" Command="{Binding SaveCommand}" 
                        BackgroundColor="{StaticResource Primary}" TextColor="White" />
            </Grid>
            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" 
                              HorizontalOptions="Center" Margin="0,20,0,0" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
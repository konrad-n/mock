<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SledzSpecke.App.ViewModels.Procedures"
             xmlns:models="clr-namespace:SledzSpecke.App.Models"
             x:DataType="vm:OldSMKProceduresListViewModel"
             x:Class="SledzSpecke.App.Views.Procedures.OldSMKProceduresListPage"
             Title="{Binding Title}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="HeaderStyle" TargetType="Label">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="{StaticResource Primary}" />
                <Setter Property="Margin" Value="0,10,0,5" />
            </Style>

            <Style x:Key="ColumnHeaderStyle" TargetType="Label">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="{StaticResource TextMutedColor}" />
                <Setter Property="Margin" Value="0,0,5,0" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Dodaj" Command="{Binding AddProcedureCommand}" IconImageSource="add.png" />
    </ContentPage.ToolbarItems>

    <RefreshView IsRefreshing="{Binding IsRefreshing}" Command="{Binding RefreshCommand}">
        <ScrollView>
            <VerticalStackLayout Spacing="20" Padding="20">

                <!-- Informacje o module -->
                <Label Text="{Binding ModuleTitle}" FontSize="20" FontAttributes="Bold" />

                <!-- Przełącznik modułów - widoczny tylko dla specjalizacji o 2 modułach -->
                <StackLayout IsVisible="{Binding HasTwoModules}" Orientation="Horizontal" 
                             HorizontalOptions="Center" Margin="0,10" Spacing="10">
                    <Label Text="Aktywny moduł:" VerticalOptions="Center" FontAttributes="Bold" />
                    <Frame Padding="0" CornerRadius="5" BorderColor="{StaticResource Primary}" 
                           HasShadow="False" IsClippedToBounds="True">
                        <Grid ColumnDefinitions="*,*">
                            <Button Text="Podstawowy" 
                                    BackgroundColor="{Binding BasicModuleSelected, Converter={StaticResource BoolToColorConverter}}"
                                    TextColor="{Binding BasicModuleSelected, Converter={StaticResource BoolToTextColorConverter}}"
                                    Command="{Binding SelectModuleCommand}" 
                                    CommandParameter="Basic" />
                            <Button Grid.Column="1" Text="Specjalistyczny" 
                                    BackgroundColor="{Binding SpecialisticModuleSelected, Converter={StaticResource BoolToColorConverter}}"
                                    TextColor="{Binding SpecialisticModuleSelected, Converter={StaticResource BoolToTextColorConverter}}"
                                    Command="{Binding SelectModuleCommand}" 
                                    CommandParameter="Specialistic" />
                        </Grid>
                    </Frame>
                </StackLayout>

                <!-- Podsumowanie procedur -->
                <Frame BorderColor="LightGray" HasShadow="True" CornerRadius="10" Padding="15">
                    <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,Auto,Auto,Auto">
                        <Label Grid.Row="0" Grid.Column="0" Text="Liczba procedur" Style="{StaticResource ColumnHeaderStyle}" />
                        <Label Grid.Row="0" Grid.Column="1" Text="Liczba wykonanych procedur" Style="{StaticResource ColumnHeaderStyle}" />
                        <Label Grid.Row="0" Grid.Column="2" Text="Liczba zaakceptowanych procedur" Style="{StaticResource ColumnHeaderStyle}" />

                        <Label Grid.Row="1" Grid.Column="0" Text="{Binding Summary.RequiredCountA, StringFormat='A: {0}'}" />
                        <Label Grid.Row="1" Grid.Column="1" Text="{Binding Summary.CompletedCountA, StringFormat='A: {0}'}" />
                        <Label Grid.Row="1" Grid.Column="2" Text="{Binding Summary.ApprovedCountA, StringFormat='A: {0}'}" />

                        <Label Grid.Row="1" Grid.Column="0" Text="{Binding Summary.RequiredCountB, StringFormat='B: {0}'}" Margin="0,5,0,0" />
                        <Label Grid.Row="1" Grid.Column="1" Text="{Binding Summary.CompletedCountB, StringFormat='B: {0}'}" Margin="0,5,0,0" />
                        <Label Grid.Row="1" Grid.Column="2" Text="{Binding Summary.ApprovedCountB, StringFormat='B: {0}'}" Margin="0,5,0,0" />
                    </Grid>
                </Frame>

                <!-- Lista grup procedur -->
                <CollectionView ItemsSource="{Binding ProcedureGroups}">
                    <CollectionView.EmptyView>
                        <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Padding="20">
                            <Label Text="Brak procedur dla wybranego modułu" FontSize="16" HorizontalOptions="Center" TextColor="{StaticResource TextMutedColor}" />
                            <Label Text="Użyj przycisku + aby dodać nową procedurę" FontSize="14" HorizontalOptions="Center" TextColor="{StaticResource TextMutedColor}" Margin="0,10,0,0" />
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="vm:ProcedureGroupViewModel">
                            <Frame Margin="0,5" Padding="15" BorderColor="LightGray" HasShadow="True" CornerRadius="10">
                                <VerticalStackLayout>
                                    <!-- Nagłówek grupy procedur -->
                                    <Grid ColumnDefinitions="*,Auto">
                                        <VerticalStackLayout Grid.Column="0">
                                            <Label Text="{Binding Title}" FontSize="16" FontAttributes="Bold" />
                                        </VerticalStackLayout>
                                        <VerticalStackLayout Grid.Column="1">
                                            <Grid ColumnDefinitions="Auto,Auto,Auto" RowDefinitions="Auto,Auto">
                                                <Label Grid.Row="0" Grid.Column="0" Text="Wymagane" Style="{StaticResource ColumnHeaderStyle}" />
                                                <Label Grid.Row="0" Grid.Column="1" Text="Wykonane" Style="{StaticResource ColumnHeaderStyle}" />
                                                <Label Grid.Row="0" Grid.Column="2" Text="Zaakceptowane" Style="{StaticResource ColumnHeaderStyle}" />

                                                <Label Grid.Row="1" Grid.Column="0" Text="{Binding StatsInfo}" />
                                            </Grid>
                                        </VerticalStackLayout>
                                    </Grid>

                                    <Button Text="Rozwiń" Command="{Binding ToggleExpandCommand}" Margin="0,10,0,0" />

                                    <!-- Lista procedur (rozwijana) -->
                                    <VerticalStackLayout IsVisible="{Binding IsExpanded}" Margin="0,10,0,0">
                                        <!-- Nagłówki kolumn -->
                                        <Grid ColumnDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto" Margin="0,0,0,5">
                                            <Label Grid.Column="0" Text="Lp" Style="{StaticResource ColumnHeaderStyle}" />
                                            <Label Grid.Column="1" Text="Data" Style="{StaticResource ColumnHeaderStyle}" />
                                            <Label Grid.Column="2" Text="Rok" Style="{StaticResource ColumnHeaderStyle}" />
                                            <Label Grid.Column="3" Text="Kod zabiegu" Style="{StaticResource ColumnHeaderStyle}" />
                                            <Label Grid.Column="4" Text="Osoba wykonująca" Style="{StaticResource ColumnHeaderStyle}" />
                                            <Label Grid.Column="5" Text="Miejsce wykonania" Style="{StaticResource ColumnHeaderStyle}" />
                                            <Label Grid.Column="6" Text="Nazwa stażu" Style="{StaticResource ColumnHeaderStyle}" />
                                            <Label Grid.Column="7" Text="Inicjały pacjenta" Style="{StaticResource ColumnHeaderStyle}" />
                                            <Label Grid.Column="8" Text="Płeć pacjenta" Style="{StaticResource ColumnHeaderStyle}" />
                                            <Label Grid.Column="9" Text="Dane osoby wykonującej I i II asystę" Style="{StaticResource ColumnHeaderStyle}" />
                                            <Label Grid.Column="10" Text="Procedura z grupy" Style="{StaticResource ColumnHeaderStyle}" />
                                            <Label Grid.Column="11" Text="Akcje" Style="{StaticResource ColumnHeaderStyle}" />
                                        </Grid>

                                        <!-- Lista procedur -->
                                        <CollectionView ItemsSource="{Binding Procedures}">
                                            <CollectionView.EmptyView>
                                                <Label Text="Brak wykonanych procedur w tej grupie" Margin="0,10" TextColor="{StaticResource TextMutedColor}" />
                                            </CollectionView.EmptyView>
                                            <CollectionView.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid ColumnDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto" Margin="0,5">
                                                        <Label Grid.Column="0" Text="{Binding ProcedureId}" />
                                                        <Label Grid.Column="1" Text="{Binding Date, StringFormat='{0:yyyy-MM-dd}'}" />
                                                        <Label Grid.Column="2" Text="{Binding Year}" />
                                                        <Label Grid.Column="3" Text="{Binding Code}" />
                                                        <Label Grid.Column="4" Text="{Binding PerformingPerson}" />
                                                        <Label Grid.Column="5" Text="{Binding Location}" />
                                                        <Label Grid.Column="6" Text="{Binding InternshipName}" />
                                                        <Label Grid.Column="7" Text="{Binding PatientInitials}" />
                                                        <Label Grid.Column="8" Text="{Binding PatientGender}" />
                                                        <Label Grid.Column="9" Text="{Binding AssistantData}" />
                                                        <Label Grid.Column="10" Text="{Binding ProcedureGroup}" />
                                                        <HorizontalStackLayout Grid.Column="11" Spacing="5">
                                                            <Button Text="✏️" 
                                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ProcedureGroupViewModel}}, Path=EditProcedureCommand}" 
                                                                    CommandParameter="{Binding .}"
                                                                    HeightRequest="30"
                                                                    WidthRequest="30"
                                                                    Padding="0"
                                                                    CornerRadius="15"
                                                                    BackgroundColor="{StaticResource Secondary}"
                                                                    TextColor="White" />
                                                            <Button Text="🗑️" 
                                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ProcedureGroupViewModel}}, Path=DeleteProcedureCommand}" 
                                                                    CommandParameter="{Binding .}"
                                                                    HeightRequest="30"
                                                                    WidthRequest="30"
                                                                    Padding="0"
                                                                    CornerRadius="15"
                                                                    BackgroundColor="{StaticResource Danger}"
                                                                    TextColor="White" />
                                                        </HorizontalStackLayout>
                                                    </Grid>
                                                </DataTemplate>
                                            </CollectionView.ItemTemplate>
                                        </CollectionView>

                                        <!-- Przycisk dodawania procedury w tej grupie -->
                                        <Button Text="Dodaj procedurę" Command="{Binding AddProcedureCommand}" Margin="0,10,0,0" />
                                    </VerticalStackLayout>
                                </VerticalStackLayout>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </ScrollView>
    </RefreshView>
</ContentPage>